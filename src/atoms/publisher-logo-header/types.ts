import { VisualStoryConfig } from "./../../types/config";
import { Config } from "../../types/config";

export interface PublisherLogoHeaderTypes {
  config: Config;
  visualStoryConfig: VisualStoryConfig;
}
