import { config, publisherConfig, ampConfig, opts } from "./config.fixture";
import { textStory } from "./text-story.fixture";
import { storyWithManyJsEmbeds } from "./story-with-jsembeds.fixture";

export { config, publisherConfig, ampConfig, textStory, storyWithManyJsEmbeds, opts };
